#!/usr/bin/env bash

# Get all active IP addresses on the machine
ip_addresses=$(ip addr show | grep "inet " | awk '{print $2}')

# Print all IP addresses to the console
echo "Active IPv4 IP addresses:"
for ip_address in $ip_addresses; do
  echo "$ip_address"
done
